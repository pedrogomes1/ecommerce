import styled from '@mui/material/styles/styled';

const Container = styled('div')(() => ({
  display: 'flex',
  maxWidth: '50%',
  flexDirection: 'column',
  gap: '15px',
}));

export { Container };
